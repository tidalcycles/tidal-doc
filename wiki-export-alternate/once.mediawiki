{{DISPLAYTITLE:once}} 
[[Type signature|Type]]: <syntaxhighlight lang="haskell" inline>once :: ControlPattern -> IO ()</syntaxhighlight>

Play one cycle of pattern.

<syntaxhighlight lang="haskell">
once $ sound "cp"
</syntaxhighlight>

See also [[asap]].